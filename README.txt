To run:
1. Navigate to GitHub repo
2. Clone or download the repo
3. Open a terminal in the main directory of the downloaded folder and install dependencies with the command: npm install
4. Run the server on Port 8080 with the command: npm start
5. In Chrome or Firefox, go to the address: http://localhost:8080
Tested on Chrome and Firefox